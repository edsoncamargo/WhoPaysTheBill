import { StyleSheet } from 'react-native'

export default function getBackStyle() {
    return StyleSheet.create({
        buttonBack: {
            position: 'absolute',
            top: 0,
            left: 0,
        },
    })
}
